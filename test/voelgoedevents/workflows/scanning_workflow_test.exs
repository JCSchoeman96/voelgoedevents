defmodule Voelgoedevents.Workflows.ScanningWorkflowTest do
  @moduledoc "Basic tests for scanning workflows."

  use Voelgoedevents.DataCase, async: true

  describe "placeholder" do
    test "true is true" do
      assert true
    end
  end
end
