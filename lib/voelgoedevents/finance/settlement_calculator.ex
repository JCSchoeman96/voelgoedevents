defmodule Voelgoedevents.Finance.SettlementCalculator do
  @moduledoc "Calculates the net payout owed to an organizer (Phase 6)."
  def calculate_settlement(_organization, _period), do: :not_implemented
end
