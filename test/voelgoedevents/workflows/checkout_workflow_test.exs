defmodule Voelgoedevents.Workflows.CheckoutWorkflowTest do
  @moduledoc "Basic tests for checkout workflows."

  use Voelgoedevents.DataCase, async: true

  describe "placeholder" do
    test "true is true" do
      assert true
    end
  end
end
