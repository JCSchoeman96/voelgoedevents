# Workflows: Scanning & Access Sync

**Purpose (Phase 5):** Orchestration layer for access control and offline synchronization.
**Boundary:** Manages the multi-step process of resolving scan conflicts (e.g., duplicate scans, offline data merge).
**Key Workflow:** `SyncResolver` (handles offline data consolidation).
