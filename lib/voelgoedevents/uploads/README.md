# Uploads Layer (File Storage)

**Purpose:** Contains configuration and utility modules for managing file uploads (e.g., event images, ticket templates).
**Boundary:** Manages cloud storage integration (e.g., S3/Cloud Storage) typically using libraries like ExAws and handles tasks like generating pre-signed URLs for direct browser uploads.
