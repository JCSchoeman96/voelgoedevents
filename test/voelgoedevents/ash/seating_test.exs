defmodule Voelgoedevents.Ash.SeatingTest do
  @moduledoc "Basic tests for seating domain."

  use Voelgoedevents.DataCase, async: true

  describe "placeholder" do
    test "true is true" do
      assert true
    end
  end
end
