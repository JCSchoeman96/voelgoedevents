defmodule Voelgoedevents.Ash.EventsTest do
  @moduledoc "Basic tests for events domain."

  use Voelgoedevents.DataCase, async: true

  describe "placeholder" do
    test "true is true" do
      assert true
    end
  end
end
