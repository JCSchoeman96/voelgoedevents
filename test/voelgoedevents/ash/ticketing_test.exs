defmodule Voelgoedevents.Ash.TicketingTest do
  @moduledoc "Basic tests for ticketing domain."

  use Voelgoedevents.DataCase, async: true

  describe "placeholder" do
    test "true is true" do
      assert true
    end
  end
end
