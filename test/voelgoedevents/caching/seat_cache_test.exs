defmodule Voelgoedevents.Caching.SeatCacheTest do
  @moduledoc "Basic tests for seat cache behaviour."

  use Voelgoedevents.DataCase, async: true

  describe "placeholder" do
    test "true is true" do
      assert true
    end
  end
end
