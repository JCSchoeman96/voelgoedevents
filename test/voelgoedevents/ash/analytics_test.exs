defmodule Voelgoedevents.Ash.AnalyticsTest do
  @moduledoc "Basic tests for analytics domain."

  use Voelgoedevents.DataCase, async: true

  describe "placeholder" do
    test "true is true" do
      assert true
    end
  end
end
