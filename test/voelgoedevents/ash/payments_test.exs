defmodule Voelgoedevents.Ash.PaymentsTest do
  @moduledoc "Basic tests for payments domain."

  use Voelgoedevents.DataCase, async: true

  describe "placeholder" do
    test "true is true" do
      assert true
    end
  end
end
